# Analytics (optional). All three must be set for the tracker to render.
# Leave them unset in forks and local development to send nothing.
NEXT_PUBLIC_UMAMI_SCRIPT_URL=
NEXT_PUBLIC_UMAMI_WEBSITE_ID=
# Hostname(s) the tracker is allowed to report from, comma-separated.
NEXT_PUBLIC_GUESTBOOK_DOMAIN=
